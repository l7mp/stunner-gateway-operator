name: "Release (dev)"

on:
  workflow_dispatch:
  push:
    paths-ignore:
      - '**.md'
      - '.github/workflows/**'
      - 'doc/**'
    branches:
      - 'main'

defaults:
  run:
    shell: bash

jobs:
  run_unit_tests:
    name: Run unit tests
    uses: l7mp/stunner-gateway-operator/.github/workflows/test.yml@main

  push_to_registry:
    name: Push Docker image to DockerHub
    needs: run_unit_tests
    if: github.repository == 'l7mp/stunner-gateway-operator'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          # the Makefile stamps the build with git describe --tags;
          # the default shallow fetch has no tags and degrades it to a bare hash
          fetch-depth: 0

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: l7mp/stunner-gateway-operator
          tags: |
            type=raw,value=dev


      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build and Push
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          cache-from: type=gha,scope=manager
          cache-to: type=gha,mode=max,scope=manager
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
